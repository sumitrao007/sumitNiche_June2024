
// Operators
// Arithmathic => +,*,/,-,%
// logical => &&,||,!
// bitwise => &,|,~,^,>>,<<
// relational => <,>,<=,>=,!=,==(It is check only value),===(strongly equality => It checks data type as well as value )
// ternary => condition?Expression1:expression2
//unary => pre/post inc/dec ++a,a--
// assignment=> =,+=,-=,*=,/=

// Sequential Statement
// if,if-else,nested if-else,switch,break,continue

    var a=10;

    // if(a<20){
    //     console.log("Condition is true");
    // }else{
    //     console.log("Condition is False");
    // }

    var choice=2;

    // switch(choice){
    //     case 1: console.log("U r in case 1");
    //             break;
    //     case 2: console.log("U r in case 2");
    //             break;
    //     default : console.log("U r in default case ");
    //              break;
    // }

    // loop statement
    // for loop ,while,do-while,foreach
    var count=5;
    // while(count!=0){
    //     console.log("Count is "+count);
    //     count--;
    // }

    // do{
    //     console.log("Count is "+count);
    //     count--;
    // }while(count!=0)

    // for(var i=0;i<5;i++){
    //    console.log("value of i is "+i) 
    // }

    // console.log("After the for loop i value is "+i);

    // var => scope => it is global scope 

    // let => it is also used to define a variable 
    // scope => within a nearest block
    
    for(let i=0;i<5;i++){
        console.log("value of i is "+i) 
     }

    //  console.log("After the for loop i value is "+i);

    // const => it is also used to define a variable
    // u can make value as constant
    // scope => global as well as local 

    const pi=3.14;
   
